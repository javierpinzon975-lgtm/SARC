spring:
  datasource.url: jdbc:h2:mem:sarc;MODE=MySQL;DB_CLOSE_DELAY=-1
  datasource.driver-class-name: org.h2.Driver
  datasource.username: sa
  datasource.password:
  jpa.hibernate.ddl-auto: create-drop
